Sólo se almacena el código fuente en el repositorio porque las componentes de Node, recursos, dependencias y configuraciones dependen del equipo en el que se instale 

Se agrega archivo "archivos faltantes.png" que muestra como debe quedar la carpeta real del proyecto con el src que está en el repositorio